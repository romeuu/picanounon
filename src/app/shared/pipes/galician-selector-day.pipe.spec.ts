import { GalicianSelectorDayPipe } from './galician-selector-day.pipe';

describe('GalicianSelectorDayPipe', () => {
  it('create an instance', () => {
    const pipe = new GalicianSelectorDayPipe();
    expect(pipe).toBeTruthy();
  });
});
